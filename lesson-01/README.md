# 📚 УРОК 1: ОСНОВЫ ZIG - ЛЕКЦИЯ
## 📖 ЧТО ТАКОЕ ZIG?
Zig - это компилируемый язык системного программирования, который:

Нет сборщика мусора (в отличие от Go)

Акцент на производительность и предсказуемое поведение

Метапрограммирование через comptime

Безопасность через проверки во время компиляции

## 🏗️ СТРУКТУРА ПРОГРАММЫ
```zig
// Импорт стандартной библиотеки (аналог import в Go)
const std = @import("std");

// Главная функция программы
pub fn main() void {
    // Вывод текста в консоль
    std.debug.print("Hello, World!\n", .{});
}
```
**Разбор по строкам:**

```zig
const std = @import("std");
```
**@import** - встроенная функция (все встроенные функции начинаются с @). Импортирует стандартную библиотеку Zig. Аналог `import "fmt"` в **Go**.

```zig
pub fn main() void
```
**pub** - модификатор доступа (public). Модификатор доступа - это ключевое слово, которое определяет видимость функции или переменной из других частей программы. В Zig есть два основных модификатора:

```zig
// Публичная (public) - видна везде
pub fn publicFunction() void { }

// Приватная (private) - видна только в текущем файле  
fn privateFunction() void { }
```

**Зачем `pub` у функции `main`?**

Функция `main` должна быть публичной, потому что:
1. Точка входа программы - операционная система должна иметь доступ к функции `main`, чтобы запустить твою программу.
2. Внешний вызов - код вне твоего модуля (сама система исполнения) вызывает `main`.
3. Стандарт языка - так требует спецификация Zig.

**Аналогия из Go:**

В Go все функции, начинающиеся с заглавной буквы, экспортируются (публичные). В Zig для этого используется явное ключевое слово `pub`.

- **fn** - объявление функции
- **main** - имя функции (точка входа)
- **void** - тип возвращаемого значения (ничего)

```zig
std.debug.print("текст\n", .{});
```

- **std.debug.print** - функция вывода (аналог `fmt.Print` в Go)

- **"текст\n"** - строка формата

- **.{}** - кортеж аргументов для подстановки

## 🔧 КОМПИЛЯЦИЯ И ЗАПУСК
```bash
# Запуск без создания файла
zig run program.zig

# Компиляция в бинарник
zig build-exe program.zig

# Запуск бинарника
./program
```
## 📝 ОСНОВНОЙ СИНТАКСИС

```zig
// Константы:

const name = "Name";          // Константа (вывод типа)
const age: u32 = 25;          // Константа с явным типом

// Переменные:
var counter: i32 = 0;           // Переменная (можно изменять)
```
// Типы данных:

- **i8, i16, i32, i64** - знаковые целые.
- **u8, u16, u32, u64** - беззнаковые целые.
- **f32, f64** - числа с плавающей точкой.
- **bool** - логический тип.
- **void** - отсутствие значения.


## 💻 ПРАКТИЧЕСКИЕ ЗАДАЧИ

**ЗАДАЧА 1.1: БАЗОВАЯ ПРОГРАММА**
Файл: `lesson-01/hello.zig`

```zig
const std = @import("std");

pub fn main() void {
    // Добавь код который выводит "Привет, я изучаю Zig!"
}
```
**ЗАДАЧА 1.2: ПЕРЕМЕННЫЕ И ВЫВОД**
Файл: `lesson-01/variables.zig`

```zig
const std = @import("std");

pub fn main() void {
    const name = "ТвоёИмя";
    const goExperience: u8 = 2; // Опыт в Go в годах
    const reason = "интерес к системному программированию";
    
    // Выведи: 
    // "Меня зовут [name]"
    // "Опыт в Go: [goExperience] лет"  
    // "Изучаю Zig потому что: [reason]"
}
```
**ЗАДАЧА 1.3: ЭКСПЕРИМЕНТЫ С PRINT**
Файл: `lesson-01/experiment.zig`

```zig
const std = @import("std");

pub fn main() void {
    const number: i32 = 42;
    const pi: f64 = 3.14159;
    const isLearning: bool = true;
    
    // Экспериментируй:
    // 1. Выведи все переменные в одной строке

    // 2. Попробуй убрать .{} - что происходит?
    // 3. Попробуй изменить формат вывода
}
```
## ❓ ВОПРОСЫ ДЛЯ ПРОВЕРКИ ЗНАНИЙ

**ТЕОРЕТИЧЕСКИЕ ВОПРОСЫ:**

- Чем отличается объявление функции в Zig от Go?
  - Ответ: названием и использованием pub вместо заглавыных букв
- Что означает символ @ перед import?
  - Ответ: импорт из стандартной библиотеки

- Почему std.debug.print требует второй аргумент .{}?
  - Ответ: чтобы функция смогла подставить значения в строку
- Какая разница между const и var?
  - Ответ: в const не изменяемое значение, в var можно присваивать новые значение

- Что такое void и когда он используется?
  - Ответ: означает что функция не возвращает значения

**ПРАКТИЧЕСКИЕ ВОПРОСЫ:**

- Что произойдет если убрать pub перед fn main?
  - Ответ: программа не скампилируется и компилятор выведит ошибку
- Как вывести число с плавающей точкой?
  - Ответ: 
  ```zig 
  std.debug.print("{f}\n", .{3.14});
  ```

- Можно ли использовать std.debug.print без второго аргумента?
  - Ответ: нет

